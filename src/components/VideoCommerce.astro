---
import FloatingVideoBubble from "./video/FloatingVideoBubble.astro";
import VideoStoriesModal from "./video/VideoStoriesModal.astro";

interface Props {
  previewUrl: string;
  videosData?: Array<{ url: string; title?: string }>;
  videoUrls?: string[];
  fullUrl?: string;
  tooltipText?: string;
  positionClasses?: string;
  showCta?: boolean;
  ctaText?: string;
  ctaLink?: string;
  loop?: boolean;
  requireScrollOnMobile?: boolean;
  dismissible?: boolean;
  storageKey?: string;
}

const props = Astro.props;
---

<FloatingVideoBubble {...props} />
<VideoStoriesModal defaultCtaText={props.ctaText} defaultCtaLink={props.ctaLink} />
